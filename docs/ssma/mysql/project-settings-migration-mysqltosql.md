---
title: プロジェクトの設定 (移行) (MySQLToSQL) |Microsoft Docs
ms.prod: sql
ms.custom: ''
ms.date: 01/19/2017
ms.reviewer: ''
ms.technology: ssma
ms.topic: conceptual
ms.assetid: 2a3cba9e-cd54-4a8b-b858-8fc4cf2580d9
author: Shamikg
ms.author: Shamikg
ms.openlocfilehash: 2f3c989626f36c003937723869b5e17d1a405ea9
ms.sourcegitcommit: b2464064c0566590e486a3aafae6d67ce2645cef
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 07/15/2019
ms.locfileid: "67908865"
---
# <a name="project-settings-migration-mysqltosql"></a>プロジェクトの設定 (移行) (MySQLToSQL)
移行 ページ、**プロジェクト設定** ダイアログ ボックスにどのように SSMA データが移行 MySQL から SQL Server をカスタマイズする設定が含まれています。  
  
移行のウィンドウが表示されます、**プロジェクト設定**と**プロジェクト設定の既定の** ダイアログ ボックス。  
  
-   上のすべての SSMA プロジェクトの設定を指定する、**ツール**メニューの [**プロジェクト設定の既定の**、プロジェクトの種類を選択します**移行ターゲット バージョン**のドロップダウン。設定にアクセスします] をクリックする**全般**クリックし、左側のウィンドウの下部にある**移行**します。  
  
-   現在のプロジェクトの設定を指定する、**ツール**メニューの [**プロジェクトの設定**、] をクリックして**全般**左側のウィンドウの下部にあるをクリックして**移行**します。  
  
## <a name="options"></a>および  
  
### <a name="bulk-copy"></a>一括コピー  
  
|項目|定義|  
|--------|--------------|  
|**バッチ サイズ**|バッチを指定します。 データ移行中に使用されるサイズ。<br /><br />**既定のモード**:1000<br /><br />**オプティミスティック モード**:1000<br /><br />**フル モード**:1000|  
|**CHECK 制約**|SQL Server テーブルにデータが挿入されると、SSMA が制約をチェックするかどうかを指定します。<br /><br />**既定のモード**:False<br /><br />**オプティミスティック モード**:False<br /><br />**フル モード**:False|  
|**[トリガーを起動する]**|SQL Server テーブルにデータを追加する場合に、SSMA が挿入トリガーを起動するかどうかを指定します。<br /><br />**既定のモード**:False<br /><br />**オプティミスティック モード**:False<br /><br />**フル モード**:False|  
|**[ID を保持する]**|SSMA では、SQL Server にデータを追加する場合に、MySQL の id 値が保持されるかどうかを指定します。 値 False の場合、変換先によって割り当てられる id 値。<br /><br />**既定のモード**:True<br /><br />**オプティミスティック モード**:True<br /><br />**フル モード**:True|  
|**[NULL を保持する]**|SSMA では、SQL Server で指定されている既定値に関係なく、SQL Server にデータを追加する場合に、ソース データ内の null 値が保持されるかどうかを指定します。<br /><br />**既定のモード**:True<br /><br />**オプティミスティック モード**:True<br /><br />**フル モード**:True|  
|**[テーブル ロック]**|データ移行中にデータをテーブルに追加する場合、SSMA でテーブルをロックするかどうかを指定します。 一括コピー操作の実行中には、一括更新ロックを取得します。 値が False の場合、ロックは行レベルで設定されます。<br /><br />**既定のモード**:False<br /><br />**オプティミスティック モード**:False<br /><br />**フル モード**:False|  
  
### <a name="data-modification"></a>データの変更  
  
|項目|定義|  
|--------|--------------|  
|**無効な日付の移行**|移行する方法を指定のように無効な日付 '2007-04-23' または' 2000-06-31 10時 00分: 00' 日付、および日時の形式でします。<br /><br />**既定のモード**:NULL に設定します。<br /><br />**オプティミスティック モード**:NULL に設定します。<br /><br />**フル モード**:NULL に設定します。|  
|**負の時間値の移行**|ように負の値を移行する方法を指定します '-30:11:00' で時刻の列。<br /><br />**既定のモード**:NULL に設定します。<br /><br />**オプティミスティック モード**:NULL に設定します。<br /><br />**フル モード**:NULL に設定します。|  
|**時刻値 24 時間での移行**|時刻値の複数の '23 を移行する方法を指定します: 順' で時刻の列。<br /><br />**既定のモード**:NULL に設定します。<br /><br />**オプティミスティック モード**:NULL に設定します。<br /><br />**フル モード**:NULL に設定します。|  
|**列に収まりきらないバイナリ値を切り捨てる**|場合は、SSMA は MySQL から SQL テーブルの列に収まり切らないバイナリの値は切り捨てられ、適切なエラー メッセージを生成します。 If No, 行にエラーが発生します。<br /><br />**既定のモード**:いいえ<br /><br />**オプティミスティック モード**:いいえ<br /><br />**フル モード**:いいえ|  
|**列に収まりきらない文字値を切り捨てる**|SSMA は、MySQL から SQL テーブルの列に収まり切らない文字値は切り捨てられ、適切なエラー メッセージを生成します。<br /><br />**既定のモード**:いいえ<br /><br />**オプティミスティック モード**:いいえ<br /><br />**フル モード**:いいえ|  
|**ゼロの日付の移行**|ゼロの日付のように移行する方法を指定します '0000-00-00' または' 0000-00-00 00時 00分: 00' の日付、および日時の列。<br /><br />**既定のモード**:NULL に設定します。<br /><br />**オプティミスティック モード**:NULL に設定します。<br /><br />**フル モード**:NULL に設定します。|  
|**移行の日付で 0 します。**|ように 0 個の部分を使用して日付を移行する方法を指定します '2009-01-00' または' 2000-00-00 11時 00分: 00' の日付、および日時の列。<br /><br />**既定のモード**:NULL に設定します。<br /><br />**オプティミスティック モード**:NULL に設定します。<br /><br />**フル モード**:NULL に設定します。|  
  
### <a name="migration-engine"></a>移行エンジン  
  
|項目|定義|  
|--------|--------------|  
|**移行エンジン**|指定データベース エンジンがデータの移行中に使用します。 クライアント側のデータの移行は、SSMA のクライアントから、ソースと SQL Server にそのデータを挿入する一括データの取得を指します。 サーバー側のデータ移行は、SQL Server ボックスで、ソースからデータを取得して、余分なクライアントのホップ (パフォーマンスが向上) を回避するための SQL Server に直接挿入する SQL エージェント ジョブとして実行する SSMA データ移行エンジン (一括コピー プログラム) を参照します。<br /><br />**既定のモード**:クライアント側のデータ移行のエンジン<br /><br />**オプティミスティック モード**:クライアント側のデータ移行のエンジン<br /><br />**フル モード**:クライアント側のデータ移行のエンジン|  
  
> [!IMPORTANT]  
> ときに、**移行エンジン**にオプションが設定されている**サーバー側のデータ移行のエンジン**、オプションを設定する新しいプロジェクト**使用して 32 ビット サーバー側のデータ移行エンジン**が表示されます. これは、データを移行する 32 ビットまたは 64 ビットの一括コピー プログラム (BCP) ユーティリティを使用するかどうかを指定します。  
  
### <a name="misc"></a>その他  
  
|項目|定義|  
|--------|--------------|  
|**拡張データの移行オプション**|個別詳細 タブで、各テーブルの余分なデータ移行のオプションを示しています。<br /><br />**既定のモード**:[非表示]<br /><br />**オプティミスティック モード**:[非表示]<br /><br />**フル モード**:[非表示]|  
|**On Error**|エラーが発生したときに、データの移行を停止します。 3 つのオプションがあります。<br /><br />**移行を停止します。** データ移行操作を停止します<br /><br />**次の表に進んでください。** 現在のテーブルにデータの移行を停止し、次に進みます<br /><br />**次のバッチに進みます。** 現在のバッチにデータの移行を停止し、次に進みます<br /><br />**既定のモード**:次のバッチに進みます<br /><br />**オプティミスティック モード**:次のバッチに進みます<br /><br />**フル モード**:次のバッチに進みます|  
  
### <a name="parallel-data-migration"></a>並列のデータの移行  
  
|項目|定義|  
|--------|--------------|  
|**データの並列移行のモード**|データの並列移行を有効にする分岐のスレッドに使用するモードを指定します。 Auto モードでは、SSMA は、データを移行するために分岐 (既定では 10) のスレッドの数を選択します。 カスタム モードでは、ユーザーがデータを移行するために分岐するスレッドの数を指定できます (最小値は 1 と最大値は 100)。 現時点では、クライアント側データ移行のエンジンだけでは、並列のデータの移行をサポートします。<br /><br />**既定のモード**:Auto<br /><br />**オプティミスティック モード**:Auto<br /><br />**フル モード**:Auto|  
  
> [!IMPORTANT]  
> ときに、**並列のデータ移行モード**にオプションが設定されている**カスタム**、オプションを設定する新しいプロジェクト**Thread Count**が表示されます。 これには、データの移行に使用されるスレッドの数を指定します。  
  
### <a name="spatial-data"></a>空間データ  
  
|項目|定義|  
|--------|--------------|  
|**エラーの処理**|空間データ型の値の移行でエラーを処理する方法を指定します。 'NULL 置き換えます' が指定されている場合、エラーが発生するすべての空間値は NULL に置き換えられます。 置換は行われませんそれ以外の場合。<br /><br />**既定のモード**:エラーを生成します。<br /><br />**オプティミスティック モード**:エラーを生成します。<br /><br />**フル モード**:エラーを生成します。|  
|**値の検証**|無効な空間値を処理する方法を指定します。 'を再試行してください、有効な' が指定されている場合の試行が行わ有効にすることに無効な値を変更します。<br /><br />**既定のモード**:有効にします。<br /><br />**オプティミスティック モード**:変更しないでください。<br /><br />**フル モード**:有効にします。|  
  
