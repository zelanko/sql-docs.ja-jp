---
title: プロジェクトの設定 (移行) (OracleToSQL) |Microsoft Docs
ms.prod: sql
ms.custom: ''
ms.date: 01/19/2017
ms.reviewer: ''
ms.technology: ssma
ms.topic: conceptual
ms.assetid: fcd6b988-633b-4b2b-9f36-6368b5e86b60
author: Shamikg
ms.author: Shamikg
manager: shamikg
ms.openlocfilehash: 8c9d934cc74bbafc5dca4517a063aaeb8c49e9a1
ms.sourcegitcommit: e7d921828e9eeac78e7ab96eb90996990c2405e9
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 07/16/2019
ms.locfileid: "68266601"
---
# <a name="project-settings-migration-oracletosql"></a>プロジェクトの設定 (移行) (OracleToSQL)
[移行] ページ、**プロジェクト設定** ダイアログ ボックスには、SSMA は oracle からデータを移行する方法をカスタマイズする設定が含まれています。[!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)]します。  
  
移行ウィンドウの両方で使用できますが、**プロジェクト設定**と**プロジェクト設定の既定の** ダイアログ ボックス。  
  
-   上のすべての SSMA プロジェクトの設定を指定する、**ツール**メニューの **プロジェクト設定の既定の**設定は表示またはから変更する必要な移行プロジェクトの種類を選択します **。移行ターゲット バージョン**をクリックしてドロップダウン**全般**クリックし、左側のウィンドウの下部にある**移行**します。  
  
-   現在のプロジェクトの設定を指定する、**ツール**メニューの [**プロジェクトの設定**、] をクリックして**全般**左側のウィンドウの下部にあるをクリックして**移行**します。  
  
## <a name="migration-engine"></a>移行エンジン  
  
|項目|定義|  
|--------|--------------|  
|**移行エンジン**|指定データベース エンジンがデータの移行中に使用します。 クライアント側のデータの移行は、SSMA のクライアントから、ソースと SQL Server にそのデータを挿入する一括データの取得を指します。 サーバー側のデータ移行は、SQL Server ボックスで、ソースからデータを取得して、余分なクライアントのホップ (パフォーマンスが向上) を回避するための SQL Server に直接挿入する SQL エージェント ジョブとして実行する SSMA データ移行エンジン (一括コピー プログラム) を参照します。<br /><br />**既定のモード**:クライアント側のデータ移行のエンジン<br /><br />**オプティミスティック モード**:クライアント側のデータ移行のエンジン<br /><br />**フル モード**:クライアント側のデータ移行のエンジン|  
  
> [!IMPORTANT]  
> ときに、**移行エンジン**にオプションが設定されている**サーバー側のデータ移行のエンジン**、オプションを設定する新しいプロジェクト**使用して 32 ビット サーバー側のデータ移行エンジン**が表示されます. これは、データを移行する 32 ビットまたは 64 ビットの一括コピー プログラム (BCP) ユーティリティを使用するかどうかを指定します。  
  
## <a name="miscellaneous-options"></a>その他のオプション  
  
|項目|定義|  
|--------|--------------|  
|**バッチ サイズ**|バッチを指定します。 データ移行中に使用されるサイズ。<br /><br />**既定のモード**:10000<br /><br />**オプティミスティック モード**:10000<br /><br />**フル モード**:10000|  
|**CHECK 制約**|SQL Server テーブルにデータが挿入されると、SSMA が制約をチェックするかどうかを指定します。<br /><br />**既定のモード**:False<br /><br />**オプティミスティック モード**:False<br /><br />**フル モード**:False|  
|**データ移行のタイムアウト**|データ移行中に使用されるタイムアウトを指定します<br /><br />**既定のモード**:15<br /><br />**オプティミスティック モード**:15<br /><br />**フル モード**:15|  
|**拡張データの移行オプション**|個別詳細 タブで、各テーブルの余分なデータ移行のオプションを示しています。<br /><br />**既定のモード**:[非表示]<br /><br />**オプティミスティック モード**:[非表示]<br /><br />**フル モード**:[非表示]|  
|**[トリガーを起動する]**|SQL Server テーブルにデータを追加する場合に、SSMA が挿入トリガーを起動するかどうかを指定します。<br /><br />**既定のモード**:False<br /><br />**オプティミスティック モード**:False<br /><br />**フル モード**:False|  
|**[ID を保持する]**|SSMA では、SQL Server で指定されている既定値に関係なく、SQL Server にデータを追加する場合に、ソース データ内の null 値が保持されるかどうかを指定します。<br /><br />**既定のモード**:True<br /><br />**オプティミスティック モード**:True<br /><br />**フル モード**:False|  
|**[NULL を保持する]**|SSMA では、SQL Server で指定されている既定値に関係なく、SQL Server にデータを追加する場合に、ソース データ内の null 値が保持されるかどうかを指定します。<br /><br />**既定のモード**:True<br /><br />**オプティミスティック モード**:True<br /><br />**フル モード**:True|  
|**エラー文字列トリム操作にマークを付ける**|ターゲット列のサイズがソース文字列の長さよりも小さい場合は、値がトリミングされ、エラーとしてマークされています。<br /><br />**既定のモード**:[はい]<br /><br />**オプティミスティック モード**:はい<br /><br />**フル モード**:はい|  
|**On Error**|エラーが発生したときに、データの移行を停止します。 3 つのオプションがあります。<br /><br />**移行を停止します。** データ移行操作を停止します<br /><br />**次の表に進んでください。** 現在のテーブルにデータの移行を停止し、次に進みます<br /><br />**次のバッチに進みます。** 現在のバッチにデータの移行を停止し、次に進みます<br /><br />**既定のモード**:次のバッチに進みます<br /><br />**オプティミスティック モード**:次のバッチに進みます<br /><br />**フル モード**:次のバッチに進みます|  
|**サポートされていない日付を置き換えます**|SSMA では、最も古いより前の日付を修正する必要があるかどうかを指定します。 [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] **datetime**日付 (1753 年 1 月 1)。<br /><br />現在の日付値を保持する次のように選択します。**何**します。 [!INCLUDE[ssNoVersion](../../includes/ssnoversion-md.md)] datetime 列 01 1753 年 1 月より前に、の日付は受け入れません。 古い日付を使用する場合は、datetime 値を文字の値に変換する必要があります。<br /><br />01 1753 年 1 月より前に、の日付を NULL に変換する選択**を NULL**します。<br /><br />サポートされている日付を 01 1753 年 1 月より前に、の日付を置き換えるには選択**サポートされている日付に最も近い置き換えます**します。<br /><br />**既定のモード**:何もしない<br /><br />**オプティミスティック モード**:何もしない<br /><br />**フル モード**:サポートされている日付に最も近いで置き換えます|  
|**[テーブル ロック]**|データ移行中にデータをテーブルに追加する場合、SSMA でテーブルをロックするかどうかを指定します。 一括コピー操作の実行中には、一括更新ロックを取得します。 値が False の場合、ロックは行レベルで設定されます。<br /><br />**既定のモード**:True<br /><br />**オプティミスティック モード**:True<br /><br />**フル モード**:True|  
  
## <a name="parallel-data-migration"></a>並列のデータの移行  
  
|項目|定義|  
|--------|--------------|  
|**データの並列移行のモード**|データの並列移行を有効にする分岐のスレッドに使用するモードを指定します。 Auto モードでは、SSMA は、データを移行するために分岐 (既定では 10) のスレッドの数を選択します。 カスタム モードでは、ユーザーがデータを移行するために分岐するスレッドの数を指定できます (最小値は 1 と最大値は 100)。 現時点では、クライアント側データ移行のエンジンだけでは、並列のデータの移行をサポートします。<br /><br />**既定のモード**:Auto<br /><br />**オプティミスティック モード**:Auto<br /><br />**フル モード**:Auto|  
  
> [!IMPORTANT]  
> ときに、**並列のデータ移行モード**にオプションが設定されている**カスタム**、オプションを設定する新しいプロジェクト**Thread Count**が表示されます。 これには、データの移行に使用されるスレッドの数を指定します。  
  
